#Crie um programa que leia um número Real qualquer pelo teclado e mostre na tela a sua porção inteira.

#from math import trunc
#n = float(input('Digite um número: '))
#r = trunc(n)
#print('A parte inteira do número digitado é: {}'.format(r))

#Alterando a forma do print para a mais atualizada

from math import trunc
n = float(input('Digite um número: '))
r= trunc(n)
print(f'A parte inteira do número digitado é: {r}')

